// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'exam_review.dart';

// **************************************************************************
// CopyWithGenerator
// **************************************************************************

abstract class _$ExamReviewCWProxy {
  ExamReview id(String id);

  ExamReview examPeriodId(String examPeriodId);

  ExamReview resultId(String resultId);

  ExamReview studentId(String studentId);

  ExamReview studentName(String studentName);

  ExamReview subjectId(String subjectId);

  ExamReview subjectName(String subjectName);

  ExamReview originalScore(num? originalScore);

  ExamReview reason(String reason);

  ExamReview status(String status);

  ExamReview resolution(String? resolution);

  ExamReview resolvedScore(num? resolvedScore);

  ExamReview requestedAt(DateTime requestedAt);

  ExamReview requestedBy(String requestedBy);

  ExamReview resolvedAt(DateTime? resolvedAt);

  ExamReview resolvedBy(String? resolvedBy);

  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `ExamReview(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// ExamReview(...).copyWith(id: 12, name: "My name")
  /// ````
  ExamReview call({
    String id,
    String examPeriodId,
    String resultId,
    String studentId,
    String studentName,
    String subjectId,
    String subjectName,
    num? originalScore,
    String reason,
    String status,
    String? resolution,
    num? resolvedScore,
    DateTime requestedAt,
    String requestedBy,
    DateTime? resolvedAt,
    String? resolvedBy,
  });
}

/// Proxy class for `copyWith` functionality. This is a callable class and can be used as follows: `instanceOfExamReview.copyWith(...)`. Additionally contains functions for specific fields e.g. `instanceOfExamReview.copyWith.fieldName(...)`
class _$ExamReviewCWProxyImpl implements _$ExamReviewCWProxy {
  const _$ExamReviewCWProxyImpl(this._value);

  final ExamReview _value;

  @override
  ExamReview id(String id) => this(id: id);

  @override
  ExamReview examPeriodId(String examPeriodId) =>
      this(examPeriodId: examPeriodId);

  @override
  ExamReview resultId(String resultId) => this(resultId: resultId);

  @override
  ExamReview studentId(String studentId) => this(studentId: studentId);

  @override
  ExamReview studentName(String studentName) => this(studentName: studentName);

  @override
  ExamReview subjectId(String subjectId) => this(subjectId: subjectId);

  @override
  ExamReview subjectName(String subjectName) => this(subjectName: subjectName);

  @override
  ExamReview originalScore(num? originalScore) =>
      this(originalScore: originalScore);

  @override
  ExamReview reason(String reason) => this(reason: reason);

  @override
  ExamReview status(String status) => this(status: status);

  @override
  ExamReview resolution(String? resolution) => this(resolution: resolution);

  @override
  ExamReview resolvedScore(num? resolvedScore) =>
      this(resolvedScore: resolvedScore);

  @override
  ExamReview requestedAt(DateTime requestedAt) =>
      this(requestedAt: requestedAt);

  @override
  ExamReview requestedBy(String requestedBy) => this(requestedBy: requestedBy);

  @override
  ExamReview resolvedAt(DateTime? resolvedAt) => this(resolvedAt: resolvedAt);

  @override
  ExamReview resolvedBy(String? resolvedBy) => this(resolvedBy: resolvedBy);

  @override
  /// This function **does support** nullification of nullable fields. All `null` values passed to `non-nullable` fields will be ignored. You can also use `ExamReview(...).copyWith.fieldName(...)` to override fields one at a time with nullification support.
  ///
  /// Usage
  /// ```dart
  /// ExamReview(...).copyWith(id: 12, name: "My name")
  /// ````
  ExamReview call({
    Object? id = const $CopyWithPlaceholder(),
    Object? examPeriodId = const $CopyWithPlaceholder(),
    Object? resultId = const $CopyWithPlaceholder(),
    Object? studentId = const $CopyWithPlaceholder(),
    Object? studentName = const $CopyWithPlaceholder(),
    Object? subjectId = const $CopyWithPlaceholder(),
    Object? subjectName = const $CopyWithPlaceholder(),
    Object? originalScore = const $CopyWithPlaceholder(),
    Object? reason = const $CopyWithPlaceholder(),
    Object? status = const $CopyWithPlaceholder(),
    Object? resolution = const $CopyWithPlaceholder(),
    Object? resolvedScore = const $CopyWithPlaceholder(),
    Object? requestedAt = const $CopyWithPlaceholder(),
    Object? requestedBy = const $CopyWithPlaceholder(),
    Object? resolvedAt = const $CopyWithPlaceholder(),
    Object? resolvedBy = const $CopyWithPlaceholder(),
  }) {
    return ExamReview(
      id: id == const $CopyWithPlaceholder()
          ? _value.id
          // ignore: cast_nullable_to_non_nullable
          : id as String,
      examPeriodId: examPeriodId == const $CopyWithPlaceholder()
          ? _value.examPeriodId
          // ignore: cast_nullable_to_non_nullable
          : examPeriodId as String,
      resultId: resultId == const $CopyWithPlaceholder()
          ? _value.resultId
          // ignore: cast_nullable_to_non_nullable
          : resultId as String,
      studentId: studentId == const $CopyWithPlaceholder()
          ? _value.studentId
          // ignore: cast_nullable_to_non_nullable
          : studentId as String,
      studentName: studentName == const $CopyWithPlaceholder()
          ? _value.studentName
          // ignore: cast_nullable_to_non_nullable
          : studentName as String,
      subjectId: subjectId == const $CopyWithPlaceholder()
          ? _value.subjectId
          // ignore: cast_nullable_to_non_nullable
          : subjectId as String,
      subjectName: subjectName == const $CopyWithPlaceholder()
          ? _value.subjectName
          // ignore: cast_nullable_to_non_nullable
          : subjectName as String,
      originalScore: originalScore == const $CopyWithPlaceholder()
          ? _value.originalScore
          // ignore: cast_nullable_to_non_nullable
          : originalScore as num?,
      reason: reason == const $CopyWithPlaceholder()
          ? _value.reason
          // ignore: cast_nullable_to_non_nullable
          : reason as String,
      status: status == const $CopyWithPlaceholder()
          ? _value.status
          // ignore: cast_nullable_to_non_nullable
          : status as String,
      resolution: resolution == const $CopyWithPlaceholder()
          ? _value.resolution
          // ignore: cast_nullable_to_non_nullable
          : resolution as String?,
      resolvedScore: resolvedScore == const $CopyWithPlaceholder()
          ? _value.resolvedScore
          // ignore: cast_nullable_to_non_nullable
          : resolvedScore as num?,
      requestedAt: requestedAt == const $CopyWithPlaceholder()
          ? _value.requestedAt
          // ignore: cast_nullable_to_non_nullable
          : requestedAt as DateTime,
      requestedBy: requestedBy == const $CopyWithPlaceholder()
          ? _value.requestedBy
          // ignore: cast_nullable_to_non_nullable
          : requestedBy as String,
      resolvedAt: resolvedAt == const $CopyWithPlaceholder()
          ? _value.resolvedAt
          // ignore: cast_nullable_to_non_nullable
          : resolvedAt as DateTime?,
      resolvedBy: resolvedBy == const $CopyWithPlaceholder()
          ? _value.resolvedBy
          // ignore: cast_nullable_to_non_nullable
          : resolvedBy as String?,
    );
  }
}

extension $ExamReviewCopyWith on ExamReview {
  /// Returns a callable class that can be used as follows: `instanceOfExamReview.copyWith(...)` or like so:`instanceOfExamReview.copyWith.fieldName(...)`.
  // ignore: library_private_types_in_public_api
  _$ExamReviewCWProxy get copyWith => _$ExamReviewCWProxyImpl(this);
}

// **************************************************************************
// JsonSerializableGenerator
// **************************************************************************

ExamReview _$ExamReviewFromJson(Map<String, dynamic> json) =>
    $checkedCreate('ExamReview', json, ($checkedConvert) {
      $checkKeys(
        json,
        requiredKeys: const [
          'id',
          'examPeriodId',
          'resultId',
          'studentId',
          'studentName',
          'subjectId',
          'subjectName',
          'reason',
          'status',
          'requestedAt',
          'requestedBy',
        ],
      );
      final val = ExamReview(
        id: $checkedConvert('id', (v) => v as String),
        examPeriodId: $checkedConvert('examPeriodId', (v) => v as String),
        resultId: $checkedConvert('resultId', (v) => v as String),
        studentId: $checkedConvert('studentId', (v) => v as String),
        studentName: $checkedConvert('studentName', (v) => v as String),
        subjectId: $checkedConvert('subjectId', (v) => v as String),
        subjectName: $checkedConvert('subjectName', (v) => v as String),
        originalScore: $checkedConvert('originalScore', (v) => v as num?),
        reason: $checkedConvert('reason', (v) => v as String),
        status: $checkedConvert('status', (v) => v as String),
        resolution: $checkedConvert('resolution', (v) => v as String?),
        resolvedScore: $checkedConvert('resolvedScore', (v) => v as num?),
        requestedAt: $checkedConvert(
          'requestedAt',
          (v) => DateTime.parse(v as String),
        ),
        requestedBy: $checkedConvert('requestedBy', (v) => v as String),
        resolvedAt: $checkedConvert(
          'resolvedAt',
          (v) => v == null ? null : DateTime.parse(v as String),
        ),
        resolvedBy: $checkedConvert('resolvedBy', (v) => v as String?),
      );
      return val;
    });

Map<String, dynamic> _$ExamReviewToJson(ExamReview instance) =>
    <String, dynamic>{
      'id': instance.id,
      'examPeriodId': instance.examPeriodId,
      'resultId': instance.resultId,
      'studentId': instance.studentId,
      'studentName': instance.studentName,
      'subjectId': instance.subjectId,
      'subjectName': instance.subjectName,
      'originalScore': ?instance.originalScore,
      'reason': instance.reason,
      'status': instance.status,
      'resolution': ?instance.resolution,
      'resolvedScore': ?instance.resolvedScore,
      'requestedAt': instance.requestedAt.toIso8601String(),
      'requestedBy': instance.requestedBy,
      'resolvedAt': ?instance.resolvedAt?.toIso8601String(),
      'resolvedBy': ?instance.resolvedBy,
    };
